# AutoDaily 第三方软件许可与版权声明

版本：v1.0；发布日期：[待填写]

## 1. AutoDaily 自身依赖

AutoDaily 使用 Rust、Tauri、Vue、npm 与 Cargo 生态的第三方组件，以及可能随构建产物分发的模型、图标、字体和安装器。每个发布版本必须随安装包提供完整、可访问的第三方许可证文件和 SBOM；以该版本实际构建产物为准。不得仅凭本文件替代许可证原文。

建议发布物包含：`THIRD_PARTY_NOTICES.txt`、`sbom.spdx.json`、所有适用许可证原文，以及模型/素材来源及授权证明。若某许可证要求提供版权声明、许可证副本、源码、可替换链接方式或其他材料，均应按其原文履行。

## 2. Rust 后端依赖

本项目 Rust 后端直接使用的第三方 crate 包括但不限于：`adb_client`、`image`、`interprocess`、`lettre`、`machineid-rs`、`ort`/`ort-sys`、`reqwest`、`rhai`、`sqlx`、`tauri` 及其插件、`tokio`、`xcap` 等；它们还会带入传递依赖。`Cargo.toml`、`Cargo.lock` 和实际构建目标是审计依据，不能只列直接依赖。

发布前应在可联网、依赖完整的构建环境中，根据锁定版本生成许可证报告与 SBOM，并将所有适用许可证原文放入安装包。此次环境无法完成全量解析：Cargo 需下载尚未缓存的 crate 时发生 TLS 凭据错误；因此本文件不虚构完整许可证清单。

## 3. 商标和第三方内容

“Android”、游戏名称、设备厂商名称及第三方脚本中出现的商标、作品或服务名称归各权利人所有。本软件不因兼容、提及或用户上传内容而取得任何授权。上传者应确保其内容拥有必要权利；权利人可通过 [待填写投诉邮箱] 提交通知，运营者将依法处理。
