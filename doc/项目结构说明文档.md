# 项目结构说明文档

- 生成范围：`src-tauri/build.rs`、`src-tauri/src/**/*.rs`、`src-tauri/crates/**/src/**/*.rs`，共 236 个 Rust 文件。
- 说明：文件、结构体、枚举、trait、方法的作用均使用概述式表达，不再记录代码片段式说明。

## `src-tauri`
### `build.rs`
- 文件作用：负责构建阶段资源处理。
- 顶层函数：
  - `main`：应用入口。

## `src-tauri/crates/child_support/src`
### `domain.rs`
- 文件作用：汇总并导出当前目录下的领域模块。

## `src-tauri/crates/child_support/src/domain`
### `config.rs`
- 文件作用：负责配置相关逻辑。

### `devices.rs`
- 文件作用：负责设备相关逻辑。

### `schedule.rs`
- 文件作用：负责调度相关逻辑。

### `scripts.rs`
- 文件作用：负责脚本相关逻辑。

### `vision.rs`
- 文件作用：负责视觉相关逻辑。

## `src-tauri/crates/child_support/src`
### `infrastructure.rs`
- 文件作用：汇总并导出当前目录下的基础设施模块。

## `src-tauri/crates/child_support/src/infrastructure`
### `adb_cli_local.rs`
- 文件作用：负责本地 ADB 命令相关逻辑。

### `capture.rs`
- 文件作用：负责截图相关逻辑。

### `context.rs`
- 文件作用：负责上下文相关逻辑。

## `src-tauri/crates/child_support/src/infrastructure/context`
### `child_process.rs`
- 文件作用：负责子进程运行环境初始化。
- 顶层函数：
  - `init_environment`：初始化运行环境。

### `child_process_sec.rs`
- 文件作用：负责子进程状态、IPC 客户端与取消控制。
- 顶层函数：
  - `is_idle`：判断当前是否空闲。
  - `set_running_status`：设置运行状态。
  - `get_running_status`：读取运行状态。
  - `process_need_stop`：判断进程是否需要停止。
  - `init_ipc_client`：初始化 IPC 客户端。
  - `get_ipc_client`：读取 IPC 客户端。
  - `init_cancel_token`：初始化取消令牌。
  - `get_cancel_token`：读取取消令牌。
  - `trigger_cancel`：触发取消信号。

### `runtime_context.rs`
- 文件作用：定义运行时上下文与执行状态。
- 结构体：
  - `PolicyState`：策略运行状态。
  - `ActionState`：动作执行状态。
  - `TaskState`：任务执行状态。
  - `ExecutionState`：执行状态汇总。
  - `ObservationState`：观测状态。
  - `RuntimeContext`：运行时上下文。
- 类型别名：
  - `SharedRuntimeContext`：共享运行时上下文。
- 顶层函数：
  - `get_runtime_ctx`：读取运行时上下文。
  - `init_runtime_ctx`：初始化运行时上下文。
- `impl` 方法：
  - `impl TaskState`
    - `default`：提供默认值。
  - `impl ExecutionState`
    - `new`：创建实例。
  - `impl ObservationState`
    - `new`：创建实例。
  - `impl RuntimeContext`
    - `new`：创建实例。

## `src-tauri/crates/child_support/src/infrastructure`
### `core.rs`
- 文件作用：负责核心逻辑。

### `devices.rs`
- 文件作用：负责设备相关逻辑。

### `image.rs`
- 文件作用：负责图像相关逻辑。

### `ipc.rs`
- 文件作用：负责 IPC 相关逻辑。

## `src-tauri/crates/child_support/src/infrastructure/ipc`
### `chanel_client.rs`
- 文件作用：定义 IPC 通道客户端与消息收发逻辑。
- 结构体：
  - `IpcClient`：IPC 客户端封装。
- `impl` 方法：
  - `impl IpcClient`
    - `fmt`：格式化输出。
  - `impl IpcClient`
    - `new`：创建实例。
    - `connect_and_run`：建立连接并启动消息循环。
    - `spawn_reconnect_task`：启动重连任务。
    - `send_heart`：发送心跳消息。
    - `send_uncertain`：发送不确定消息。
    - `send_ensure`：发送确认消息。
  - `impl IpcClient`
    - `handle_msg`：处理收到的消息。

### `msg_handler_child.rs`
- 文件作用：处理子进程侧 IPC 消息。
- 顶层函数：
  - `handle_main_message`：处理主流程消息。
  - `handle_process_control`：处理进程控制。
  - `handle_session_control`：处理会话控制。
  - `handle_config_update`：处理配置更新。

### `runtime_reporter.rs`
- 文件作用：上报运行时、生命周期与调度事件。
- 顶层函数：
  - `now_millis_string`：生成当前毫秒时间串。
  - `current_session_id`：获取当前会话标识。
  - `current_script_id`：获取当前脚本标识。
  - `emit_runtime_event`：发送运行时事件。
  - `emit_lifecycle_event`：发送生命周期事件。
  - `emit_lifecycle_event_with`：发送附带数据的生命周期事件。
  - `emit_progress_event`：发送进度事件。
  - `emit_schedule_event`：发送调度事件。

## `src-tauri/crates/child_support/src/infrastructure`
### `logging.rs`
- 文件作用：负责日志相关逻辑。

## `src-tauri/crates/child_support/src/infrastructure/logging`
### `child_log.rs`
- 文件作用：负责子进程日志相关逻辑。
- `impl` 方法：
  - `impl LogChild`
    - `debug`：输出调试日志相关逻辑。
    - `info`：输出信息日志相关逻辑。
    - `warn`：输出警告日志相关逻辑。
    - `error`：输出错误日志相关逻辑。
  - `impl IpcClient`
    - `set_log_level`：设置日志级别。
    - `should_log`：判断是否需要记录日志。
    - `create_logger_and_send`：创建日志器并发送日志。
    - `debug`：输出调试日志相关逻辑。
    - `info`：输出信息日志相关逻辑。
    - `warn`：输出警告日志相关逻辑。
    - `error`：输出错误日志相关逻辑。

## `src-tauri/crates/child_support/src/infrastructure`
### `ort.rs`
- 文件作用：负责ONNX Runtime相关逻辑。

### `path_resolve.rs`
- 文件作用：负责路径resolve相关逻辑。

### `scripts.rs`
- 文件作用：负责脚本相关逻辑。

## `src-tauri/crates/child_support/src/infrastructure/scripts`
### `execution_plan.rs`
- 文件作用：负责执行计划相关逻辑。
- 结构体：
  - `ExecutionPlanSummary`：执行计划摘要。
  - `PlannedTask`：待执行任务项。
  - `SkippedTask`：跳过任务项。
  - `TaskSelection`：任务Selection定义。
  - `TemplateValuesSnapshot`：模板Values快照定义。
  - `TemplateTaskSetting`：模板任务Setting定义。
- 枚举：
  - `ExecutionPlan`：执行计划。
- `impl` 方法：
  - `impl ExecutionPlanAssembler`
    - `assemble`：处理组装。
    - `is_policy_debug_target`：判断策略调试目标。
    - `should_record_schedule`：判断记录调度。
    - `select_tasks`：筛选待执行任务。
    - `resolve_trigger_mode`：解析触发模式。
    - `parse_template_values`：解析模板values。
    - `resolve_task_enabled`：解析任务启用状态。
    - `resolve_task_cycle`：解析任务周期。
    - `should_skip_by_schedule`：判断是否因调度规则跳过。
    - `skip_reason_without_history`：生成无历史记录时的跳过原因。
    - `skip_reason_with_history`：生成带历史记录时的跳过原因。
    - `weekday_label`：处理星期标签。
  - `impl ExecutionPlan`
    - `task_selection`：处理任务selection。
    - `is_policy_debug`：判断策略调试。
    - `summary`：生成摘要相关逻辑。
    - `progress_message`：处理进度消息。
  - `impl ExecutionPlanSummary`
    - `progress_message`：处理进度消息。
    - `mode_label`：处理模式标签。
  - `impl TaskSelection`
    - `default`：提供默认值。

### `executor.rs`
- 文件作用：负责执行器相关逻辑。
- 结构体：
  - `StepFrame`：步骤Frame定义。
  - `PolicyBundle`：策略包定义。
  - `PolicyCandidate`：策略候选框定义。
  - `ActivePolicyRoundTrace`：当前策略轮次跟踪。
  - `ProgressProbe`：进度探针定义。
  - `OklabColor`：Oklab 颜色值结构。
  - `ColorCluster`：颜色Cluster定义。
  - `RuntimeTemplateValuesSnapshot`：运行时模板Values快照定义。
  - `ScriptExecutor`：脚本Executor定义。
- 枚举：
  - `ControlFlow`：流程执行器。
- `impl` 方法：
  - `impl ScriptExecutor`
    - `new`：创建实例。
    - `reset_node_indices`：重置节点indices。
    - `get_node_index`：读取节点index。
    - `set_node_index`：设置节点index。
    - `inc_node_index`：递增节点index。
    - `reset_scope`：重置作用域。
    - `execute`：执行相关逻辑。
    - `enter_step`：进入步骤。
    - `leave_step`：离开步骤。
    - `execute_step_inner`：执行步骤inner。
    - `execute_sequence`：执行步骤序列。

## `src-tauri/crates/child_support/src/infrastructure/scripts/executor`
### `action.rs`
- 文件作用：负责动作相关逻辑。
- `impl` 方法：
  - `impl ScriptExecutor`
    - `execute_action_step`：执行动作步骤。
    - `should_skip_action_execution`：判断动作是否应跳过执行。
    - `mark_action_succeeded`：标记动作succeeded。
    - `before_action`：执行动作前置处理。
    - `after_action`：执行动作后置处理。
    - `dispatch_action`：分发动作执行。
    - `execute_click`：执行点击。
    - `execute_swipe`：执行滑动。
    - `resolve_ocr_target_point`：解析 OCR 目标点。
    - `resolve_det_target_point`：解析检测目标点。
    - `build_capture_observation`：构建截图观测。
    - `activate_image_context`：激活图像上下文。
    - `activate_image_var`：激活图像变量。
    - `read_runtime_image_var`：读取运行时图像变量。
    - `ensure_screen_size`：确保screensize。
    - `point_to_absolute`：处理点toabsolute。
    - `bounding_box_center_to_point`：处理边界框centerto点。
    - `select_det_result`：选择检测结果。
    - `build_capture_cache_key`：构建截图cache键。
    - `write_hash_segment`：写入hashsegment。
    - `record_action_trace`：处理记录动作跟踪。
    - `push_active_policy_page_fingerprint`：追加当前策略分页结果指纹。
    - `current_page_fingerprint`：获取当前分页结果指纹。
    - `observe_refresh_hook`：触发观测刷新钩子。
    - `evaluate_action_progress_timeout`：评估动作进度超时。
    - `evaluate_progress_probe`：评估进度探针。
    - `record_progress_evidence`：处理记录进度evidence。
    - `sleep_with_progress_timeout`：等待with进度超时。
    - `handle_timeout_action`：处理超时动作。
    - `emit_timeout_signals`：发送超时信号。
    - `current_execution_locator`：获取当前执行定位信息。
    - `mark_current_task_skipped`：标记当前任务跳过。
    - `recovery_task_id`：处理恢复任务id。
    - `reset_progress_probe`：重置进度探针。
    - `build_page_fingerprint`：构建分页结果指纹。
    - `build_simple_action_trace`：构建简单动作跟踪。
    - `build_action_trace`：构建动作跟踪。
    - `build_action_signature`：构建动作签名。
    - `build_point_target`：构建点目标。
    - `build_ocr_target`：构建 OCR 目标。
    - `build_det_target`：构建检测目标。
    - `action_kind_code`：处理动作类型code。
    - `action_source_code`：处理动作来源code。
    - `target_role_code`：处理目标rolecode。
    - `action_requires_wait`：处理动作需要等待。
    - `percent_point_to_absolute`：处理百分比点toabsolute。

### `flow.rs`
- 文件作用：负责流程相关逻辑。
- `impl` 方法：
  - `impl ScriptExecutor`
    - `execute_flow_control_step`：执行流程控制步骤。
    - `execute_data_handling_step`：执行数据handling步骤。
    - `execute_color_compare_step`：执行颜色比较步骤。
    - `require_last_capture_image`：读取最近一次截图图像。
    - `ocr_item_matches_color`：执行 OCR项matches颜色。
    - `color_compare_threshold`：处理颜色比较threshold。
    - `extract_font_candidate_colors`：提取字体候选颜色。
    - `extract_background_color`：提取背景颜色。
    - `clamp_bbox_to_image`：限制bboxto图像。
    - `expand_rect_for_ring`：扩展环形区域矩形范围。
    - `collect_rect_pixels`：收集矩形区域pixels。
    - `collect_ring_pixels`：收集ringpixels。
    - `cluster_colors`：聚类颜色样本。
    - `mean_color`：计算平均颜色。
    - `oklab_distance`：计算 Oklab 颜色距离。
    - `rgb_to_oklab`：将 RGB 转为 Oklab。
    - `execute_task_control_step`：执行任务控制步骤。
    - `execute_vision_step`：执行视觉步骤。
    - `match_policy_set_result`：匹配策略集结果。
    - `set_state_value`：设置状态值。
    - `match_state_status`：匹配状态状态。
    - `match_state_value`：匹配状态值。
    - `add_policy_overlay`：追加策略覆盖层。
    - `match_exec_num_compare`：匹配execnum比较。
    - `current_exec_count`：获取当前执行次数。
    - `resolve_exec_limit`：解析执行上限。
    - `compare_exec_count`：处理比较exec次数。

### `policy.rs`
- 文件作用：负责策略相关逻辑。
- `impl` 方法：
  - `impl ScriptExecutor`
    - `debug_execute_policy`：输出调试日志execute策略。
    - `debug_execute_policy_group`：输出调试日志execute策略分组。
    - `debug_execute_policy_set`：输出调试日志execute策略集。
    - `execute_handle_policy_set`：执行策略集处理流程。
    - `debug_execute_policy_candidates`：输出调试日志execute策略candidates。
    - `execute_handle_policy`：执行策略处理流程。
    - `execute_policy_candidates`：执行策略candidates。
    - `execute_policy_steps`：执行策略steps。
    - `policy_is_skipped`：处理策略is跳过。
    - `mark_policy_succeeded`：标记策略succeeded。
    - `build_policy_match_flags`：构建策略matchflags。
    - `load_policy_bundle`：加载策略包。
    - `resolve_policy_set_candidates`：解析策略集候选项。
    - `collect_policy_set_targets`：收集策略集targets。
    - `resolve_policy_candidates`：解析策略候选项。
    - `resolve_policy_group_candidates`：解析策略分组选项。
    - `capture_policy_debug_observation`：处理截图策略调试观测。
    - `log_policy_debug_observation`：处理日志策略调试观测。
    - `log_policy_debug_result`：处理日志策略调试结果。
    - `format_policy_action_targets`：处理格式策略动作targets。
    - `begin_active_policy_round_trace`：开始当前策略轮次跟踪。
    - `take_active_policy_round_trace`：取出当前策略轮次跟踪。

### `runtime.rs`
- 文件作用：负责运行时相关逻辑。
- `impl` 方法：
  - `impl ScriptExecutor`
    - `set_runtime_var`：设置运行时变量。
    - `read_runtime_var`：读取运行时变量。
    - `remove_runtime_var`：移除运行时变量。
    - `hydrate_input_scope`：处理填充输入作用域。
    - `clear_input_scope`：清空输入作用域。
    - `parse_runtime_template_values`：解析运行时模板values。
    - `input_variable_visible_for_task`：处理输入变量visiblefor任务。
    - `resolve_input_variable_value`：解析输入变量值。
    - `resolve_task_default_variable_value`：解析任务默认变量值。
    - `input_variable_storage_key`：处理输入变量storage键。
    - `build_scope_root_value`：构建作用域root值。
    - `build_nested_map`：构建嵌套映射。
    - `merge_map`：合并映射。
    - `var_value_to_dynamic`：处理变量值to动态值。
    - `search_hits_to_dynamic`：将搜索命中结果转为动态值。
    - `compare_dynamic`：处理比较动态值。
    - `compare_bool`：处理比较布尔值。
    - `dynamic_eq`：处理动态值eq。
    - `dynamic_to_number`：处理动态值tonumber。
    - `dynamic_to_string`：处理动态值tostring。
    - `eval_bool`：处理求值布尔值。
    - `eval_dynamic`：处理求值动态值。
    - `state_target_label`：处理状态目标标签。
    - `execute_error`：执行错误。

### `tests.rs`
- 文件作用：负责测试辅助逻辑。
- 顶层函数：
  - `build_ocr_result`：构建 OCR 结果。
  - `build_det_result`：构建检测结果。
  - `fill_rect`：填充矩形区域。
  - `select_ocr_result_prefers_exact_match_then_contains`：优先按精确匹配再按包含关系选择 OCR 结果。
  - `select_det_result_matches_label_index`：按标签索引匹配检测结果。
  - `bounding_box_center_converts_to_device_point`：处理边界框centerconvertsto设备点。
  - `result_vec_can_be_deserialized_from_dynamic`：处理结果veccanbedeserializedfrom动态值。
  - `capture_cache_key_changes_with_signature_grid_size`：处理截图cache键changeswith签名gridsize。
  - `capture_cache_key_changes_with_model_config`：处理截图cache键changeswith模型配置。
  - `compare_optional_id_supports_eq_and_ne`：处理比较optionalidsupportseqandne。
  - `compare_bool_supports_eq_and_ne`：处理比较布尔值supportseqandne。
  - `build_task_with_variables`：构建任务withvariables。
  - `build_input_variable`：构建输入变量。
  - `build_script_level_input_variable`：构建脚本level输入变量。
  - `build_executor`：构建执行器。
  - `input_variable_prefers_template_values_by_variable_id`：处理输入变量prefers模板valuesby变量id。
  - `input_variable_falls_back_to_task_storage_key`：处理输入变量fallsbackto任务storage键。
  - `input_variable_falls_back_to_catalog_default_without_template_or_task_value`：处理输入变量fallsbackto目录defaultwithout模板or任务值。
  - `task_owned_input_variable_is_hidden_without_task_context`：处理任务owned输入变量ishiddenwithout任务上下文。
  - `timeout_handling_resets_progress_probe_after_stop_execution`：处理超时handlingresets进度探针afterstop执行。
  - `color_compare_matches_background_ring_color`：处理颜色比较matchesbackgroundring颜色。
  - `color_compare_matches_font_color_against_top_three_clusters`：处理颜色比较matchesfont颜色against置顶threeclusters。

## `src-tauri/crates/child_support/src/infrastructure/scripts`
### `schedule_journal.rs`
- 文件作用：负责调度journal相关逻辑。
- `impl` 方法：
  - `impl ScheduleJournal`
    - `task_cycle_value`：处理任务cycle值。
    - `append_task_record`：追加任务记录。
    - `load_latest_success_record`：加载latestsuccess记录。

### `scheduler.rs`
- 文件作用：负责调度器相关逻辑。
- 结构体：
  - `ParsedScriptBundle`：Parsed脚本包定义。
  - `ScriptScheduler`：脚本Scheduler定义。
- 顶层函数：
  - `init_scheduler`：初始化调度器。
  - `get_scheduler`：读取调度器。
- `impl` 方法：
  - `impl ScriptScheduler`
    - `configure_visual_services`：配置visualservices。
    - `load_script_bundle`：加载脚本包。
    - `current_run_target`：获取当前运行目标。
    - `consume_task_skip_flag`：消费任务跳过标记。
    - `mark_task_succeeded`：标记任务succeeded。
    - `load_session`：加载会话。
    - `current_script`：获取当前脚本。
    - `current_script_snapshot`：获取当前脚本快照。
    - `queue_len`：处理队列len。
    - `tick`：推进一次调度轮询。
    - `execute_script`：执行脚本。
    - `execute_debug_policy_target`：执行调试策略目标。
    - `clear_queue`：清空任务队列。
    - `clear_session`：清空会话状态。

## `src-tauri/crates/child_support/src/infrastructure`
### `session.rs`
- 文件作用：负责会话相关逻辑。

## `src-tauri/crates/child_support/src/infrastructure/session`
### `runtime_session.rs`
- 文件作用：负责运行时会话相关逻辑。
- 结构体：
  - `ChildRuntimeSession`：子进程运行时会话定义。
  - `ChildRuntimeSessionSummary`：子进程运行时会话摘要定义。
- 类型别名：
  - `SharedChildRuntimeSession`：共享子进程运行时会话定义。
- 顶层函数：
  - `get_runtime_session_store`：读取运行时会话store。
  - `replace_runtime_session`：替换运行时会话。
  - `clear_runtime_session`：清空运行时会话。
  - `get_script_bundle_snapshot`：读取脚本包快照。
  - `get_runtime_queue_item`：读取运行时队列项。
  - `get_runtime_execution_policy`：读取运行时执行策略。
  - `try_current_session_summary`：尝试当前会话摘要。
- `impl` 方法：
  - `impl ChildRuntimeSession`
    - `new`：创建实例。
    - `summary`：生成摘要相关逻辑。
    - `bundle`：处理包。
    - `queue_item`：处理队列项。
    - `runtime_policy`：处理运行时策略。

## `src-tauri/crates/child_support/src/infrastructure`
### `store_local.rs`
- 文件作用：负责本地存储相关逻辑。

### `vision.rs`
- 文件作用：负责视觉相关逻辑。

## `src-tauri/crates/child_support/src`
### `lib.rs`
- 文件作用：负责库入口与公共导出。

## `src-tauri/crates/runtime_common/src`
### `core.rs`
- 文件作用：负责核心逻辑。
- 类型别名：
  - `DeviceId`：设备Id定义。
  - `ScriptId`：脚本Id定义。
  - `UserId`：用户Id定义。
  - `TaskId`：任务Id定义。
  - `GuardId`：GuardId定义。
  - `PolicyId`：策略Id定义。
  - `PolicyGroupId`：策略分组Id定义。
  - `PolicySetId`：策略集Id定义。
  - `SubFlowId`：Sub流程Id定义。
  - `StepId`：步骤Id定义。
  - `MessageId`：消息Id定义。
  - `SessionId`：会话Id定义。
  - `ExecutionId`：执行标识类型。
  - `ScheduleId`：调度Id定义。
  - `TemplateId`：模板Id定义。
  - `ScriptTemplateValueId`：脚本模板值Id定义。
  - `AccountId`：AccountId定义。
  - `WithoutGenerics`：WithoutGenerics定义。
  - `OptionInnerType`：OptionInnerType类型。
- `impl` 方法：
  - `impl UuidV7`
    - `decode`：执行解码。
  - `impl UuidV7`
    - `type_info`：返回类型信息。
    - `compatible`：判断兼容性。
  - `impl UuidV7`
    - `new_v7`：创建v7。
    - `to_string`：转为字符串。
    - `as_uuid`：转换UUID。
  - `impl UuidV7`
    - `fmt`：格式化输出。
  - `impl UuidV7`
    - `from`：从其他类型转换。
  - `impl Uuid`
    - `from`：从其他类型转换。
  - `impl UuidV7`
    - `name`：返回名称相关逻辑。
    - `inline`：生成内联表示。
    - `visit_dependencies`：遍历依赖项。
    - `visit_generics`：遍历泛型参数。
    - `output_path`：处理输出路径。

## `src-tauri/crates/runtime_common/src/core`
### `core_error.rs`
- 文件作用：负责core错误相关逻辑。
- 枚举：
  - `CoreError`：Core错误枚举。

### `cores_affinity.rs`
- 文件作用：负责coresaffinity相关逻辑。
- 结构体：
  - `CurrentProcess`：当前进程定义。
- 顶层函数：
  - `set_process_affinity`：设置进程affinity。
  - `set_cpu_affinity`：设置 CPU 亲和性。

### `time_format.rs`
- 文件作用：负责time格式相关逻辑。
- 枚举：
  - `LocalTimer`：LocalTimer枚举。
- `impl` 方法：
  - `impl LocalTimer`
    - `format_time`：处理格式time。

## `src-tauri/crates/runtime_common/src`
### `ipc.rs`
- 文件作用：负责 IPC 相关逻辑。

## `src-tauri/crates/runtime_common/src/ipc`
### `channel_error.rs`
- 文件作用：负责通道错误相关逻辑。
- 枚举：
  - `ChannelError`：通道错误枚举。

### `message.rs`
- 文件作用：负责消息相关逻辑。
- 结构体：
  - `IpcMessage`：IPC消息定义。
  - `ProcessControlMessage`：进程控制消息定义。
  - `RuntimeVisionTextCachePolicy`：运行时视觉TextCache策略定义。
  - `RuntimeExecutionPolicy`：运行时执行策略定义。
  - `RuntimeQueueItem`：运行时队列项。
  - `ScriptBundleSnapshot`：脚本包快照定义。
  - `RuntimeSessionSnapshot`：运行时会话快照定义。
  - `RuntimeLifecycleEvent`：运行时LifecycleEvent定义。
  - `RuntimeProgressEvent`：运行时进度Event定义。
  - `RuntimeScheduleEvent`：运行时调度Event定义。
  - `ConfigUpdateMessage`：配置更新消息定义。
  - `LogMessage`：日志消息定义。
  - `HeartbeatMessage`：Heartbeat消息定义。
  - `ErrorMessage`：错误消息定义。
- 枚举：
  - `MessageType`：消息Type类型。
  - `MessagePayload`：消息载荷。
  - `ProcessAction`：进程动作枚举。
  - `RunTarget`：运行目标枚举。
  - `TimeoutAction`：超时动作枚举。
  - `TimeoutNotifyChannel`：超时Notify通道枚举。
  - `SessionControlMessage`：会话控制消息枚举。
  - `RuntimeLifecyclePhase`：运行时LifecyclePhase枚举。
  - `RuntimeProgressPhase`：运行时进度Phase枚举。
  - `RuntimeScheduleStatus`：运行时调度状态。
  - `RuntimeEventMessage`：运行时Event消息枚举。
  - `ConfigUpdateType`：配置更新Type类型。
- `impl` 方法：
  - `impl IpcMessage`
    - `new`：创建实例。
    - `create_response`：创建response。
  - `impl RunTarget`
    - `script_id`：处理脚本id。

## `src-tauri/crates/runtime_common/src`
### `lib.rs`
- 文件作用：负责库入口与公共导出。

### `logging.rs`
- 文件作用：负责日志相关逻辑。

## `src-tauri/crates/runtime_common/src/logging`
### `log_level.rs`
- 文件作用：负责日志level相关逻辑。
- 枚举：
  - `LogLevel`：日志Level枚举。
- `impl` 方法：
  - `impl LogLevel`
    - `from`：从其他类型转换。
  - `impl LogLevel`
    - `fmt`：格式化输出。

### `log_trait.rs`
- 文件作用：负责日志trait相关逻辑。
- 枚举：
  - `LoggerInitError`：Logger初始化错误枚举。
- Trait：
  - `LogTrait`：日志Trait枚举。
- 顶层函数：
  - `debug`：输出调试日志相关逻辑。
  - `info`：输出信息日志相关逻辑。
  - `warn`：输出警告日志相关逻辑。
  - `error`：输出错误日志相关逻辑。
- `impl` 方法：
  - `impl Log`
    - `init_logger`：初始化日志记录器。
    - `debug`：输出调试日志相关逻辑。
    - `info`：输出信息日志相关逻辑。
    - `warn`：输出警告日志相关逻辑。
    - `error`：输出错误日志相关逻辑。

## `src-tauri/crates/runtime_engine/src`
### `api.rs`
- 文件作用：负责接口相关逻辑。

## `src-tauri/crates/runtime_engine/src/api`
### `api_response.rs`
- 文件作用：负责接口响应相关逻辑。
- 结构体：
  - `ApiResponse`：ApiResponse响应。
- `impl` 方法：
  - `impl Serialize`
    - `ok`：构造成功结果。
    - `error`：输出错误日志相关逻辑。
    - `success`：构造成功响应。
    - `failed`：构造失败响应。
  - `impl ApiResponse<T>`
    - `from`：从其他类型转换。

### `backend_dto.rs`
- 文件作用：负责后端dto相关逻辑。
- 结构体：
  - `AuthRes`：Auth响应。
  - `BackendApiRes`：后端Api响应。
  - `RefreshTokenReq`：Refresh令牌请求。

## `src-tauri/crates/runtime_engine/src`
### `app.rs`
- 文件作用：负责应用相关逻辑。

## `src-tauri/crates/runtime_engine/src/app`
### `app_error.rs`
- 文件作用：负责app错误相关逻辑。
- 枚举：
  - `AppError`：App错误枚举。

## `src-tauri/crates/runtime_engine/src`
### `constant.rs`
- 文件作用：负责常量定义。

### `domain.rs`
- 文件作用：汇总并导出当前目录下的领域模块。

## `src-tauri/crates/runtime_engine/src/domain`
### `config.rs`
- 文件作用：负责配置相关逻辑。

## `src-tauri/crates/runtime_engine/src/domain/config`
### `notice_conf.rs`
- 文件作用：负责notice配置相关逻辑。
- 结构体：
  - `ResolvedSmtpServer`：ResolvedSmtp服务定义。
  - `EmailConfig`：Email配置。
- 枚举：
  - `EmailProviderPreset`：Email提供方Preset枚举。
  - `EmailSecurity`：EmailSecurity枚举。
- 顶层函数：
  - `resolves_builtin_provider_profile`：解析内置提供方配置。
  - `splits_multiple_recipients`：拆分多个收件人。
- `impl` 方法：
  - `impl EmailConfig`
    - `default`：提供默认值。
  - `impl EmailProviderPreset`
    - `default`：提供默认值。
  - `impl EmailSecurity`
    - `default`：提供默认值。
  - `impl EmailProviderPreset`
    - `preset_server`：返回预设服务配置。
  - `impl EmailConfig`
    - `resolved_server`：返回解析后的服务配置。
    - `sender_email_value`：读取发送人邮箱值。
    - `sender_name_value`：读取发送人名称值。
    - `recipient_list`：读取收件人列表。
    - `timeout_seconds_value`：处理超时seconds值。

### `scripts_conf.rs`
- 文件作用：负责scripts配置相关逻辑。
- 结构体：
  - `ScriptsConfig`：Scripts配置。
- `impl` 方法：
  - `impl ScriptsConfig`
    - `default`：提供默认值。

### `shortcut_conf.rs`
- 文件作用：负责快捷键配置相关逻辑。
- 结构体：
  - `ShortCut`：快捷键定义。
- `impl` 方法：
  - `impl ShortCut`
    - `default`：提供默认值。

### `sys_conf.rs`
- 文件作用：负责sys配置相关逻辑。
- 结构体：
  - `SystemConfig`：系统配置。
- 枚举：
  - `StartMode`：启动模式。
  - `IdleAction`：空闲动作枚举。
- `impl` 方法：
  - `impl SystemConfig`
    - `default`：提供默认值。

### `vision_cache_conf.rs`
- 文件作用：负责视觉cache配置相关逻辑。
- 结构体：
  - `VisionTextCacheConfig`：视觉TextCache配置。
  - `VisionTextCacheRuntimeConfig`：视觉TextCache运行时配置。
- `impl` 方法：
  - `impl VisionTextCacheConfig`
    - `to_runtime_config`：转换为运行时配置。
  - `impl VisionTextCacheConfig`
    - `default`：提供默认值。

## `src-tauri/crates/runtime_engine/src/domain`
### `devices.rs`
- 文件作用：负责设备相关逻辑。

## `src-tauri/crates/runtime_engine/src/domain/devices`
### `device_conf.rs`
- 文件作用：负责设备配置相关逻辑。
- 结构体：
  - `DeviceTable`：设备表定义。
  - `DeviceConfig`：设备配置。
  - `DeviceExecutionPolicy`：设备执行策略定义。
- 枚举：
  - `TimeoutAction`：超时动作枚举。
  - `TimeoutNotifyChannel`：超时Notify通道枚举。
  - `CapMethod`：截图方式定义。
  - `DevicePlatform`：设备平台枚举。
- `impl` 方法：
  - `impl DeviceTable`
    - `default`：提供默认值。
  - `impl DeviceConfig`
    - `default`：提供默认值。
  - `impl DevicePlatform`
    - `default`：提供默认值。
  - `impl DeviceExecutionPolicy`
    - `default`：提供默认值。

### `device_schedule.rs`
- 文件作用：负责设备调度相关逻辑。
- 结构体：
  - `DeviceScriptAssignment`：设备脚本分配定义。
  - `DeviceScriptSchedule`：设备脚本调度定义。
- 枚举：
  - `TaskCycle`：任务Cycle枚举。
  - `RunStatus`：运行状态。
- `impl` 方法：
  - `impl DeviceScriptAssignment`
    - `default`：提供默认值。

## `src-tauri/crates/runtime_engine/src/domain`
### `schedule.rs`
- 文件作用：负责调度相关逻辑。

## `src-tauri/crates/runtime_engine/src/domain/schedule`
### `script_time_template_values.rs`
- 文件作用：负责脚本time模板values相关逻辑。
- 结构体：
  - `ScriptTimeTemplateValuesDto`：脚本时间模板值传输结构。
- `impl` 方法：
  - `impl ScriptTimeTemplateValuesDto`
    - `default`：提供默认值。

### `time_template.rs`
- 文件作用：负责time模板相关逻辑。
- 结构体：
  - `TimeTemplate`：时间模板定义。
- `impl` 方法：
  - `impl TimeTemplate`
    - `default`：提供默认值。

## `src-tauri/crates/runtime_engine/src/domain`
### `scripts.rs`
- 文件作用：负责脚本相关逻辑。

## `src-tauri/crates/runtime_engine/src/domain/scripts`
### `nodes.rs`
- 文件作用：负责节点定义相关逻辑。

## `src-tauri/crates/runtime_engine/src/domain/scripts/nodes`
### `action.rs`
- 文件作用：负责动作相关逻辑。
- 枚举：
  - `SwipeMode`：滑动模式定义。
  - `ClickMode`：点击模式定义。
  - `Action`：动作定义。

### `data_handing.rs`
- 文件作用：负责数据handing相关逻辑。
- 结构体：
  - `ColorRgb`：颜色RGB定义。
- 枚举：
  - `DataHanding`：数据Handing枚举。
  - `FilterMode`：过滤模式定义。
  - `ColorCompareMethod`：颜色比较方式枚举。
  - `VarValue`：变量值定义。

### `flow_control.rs`
- 文件作用：负责流程控制相关逻辑。
- 枚举：
  - `FlowControl`：流程控制枚举。
  - `ConditionNode`：条件节点定义。
  - `PolicySetResultField`：策略集结果字段枚举。
  - `PolicySetResultCompareOp`：策略集结果比较运算符枚举。
  - `CompareOp`：比较运算符枚举。

### `policy_execution.rs`
- 文件作用：负责策略执行相关逻辑。
- 结构体：
  - `PolicyActionTarget`：策略动作目标定义。
  - `PolicyActionTrace`：策略动作跟踪定义。
  - `PolicyExecutionRound`：策略执行轮次定义。
  - `PolicyExecutionResult`：策略执行结果。
- 枚举：
  - `PolicyActionKind`：策略动作类型。
  - `PolicyActionSource`：策略动作来源枚举。
  - `PolicyActionTargetRole`：策略动作目标角色枚举。

### `task_control.rs`
- 文件作用：负责任务控制相关逻辑。
- 枚举：
  - `TaskControl`：任务控制枚举。
  - `StateTarget`：状态目标定义。
  - `StateStatus`：状态标记定义。

### `vision_node.rs`
- 文件作用：负责视觉节点相关逻辑。
- 枚举：
  - `VisionNode`：视觉节点。

## `src-tauri/crates/runtime_engine/src/domain/scripts`
### `point.rs`
- 文件作用：负责点相关逻辑。
- 结构体：
  - `Point`：点坐标结构。
  - `Rotation`：旋转角度定义。
  - `Line`：直线结构。
  - `PointU16`：点坐标结构。
  - `PointF32`：点坐标结构。
- 类型别名：
  - `Output`：输出类型。
- `impl` 方法：
  - `impl Point<T>`
    - `new`：创建实例。
  - `impl Point<T>`
    - `add`：执行加法。
  - `impl Point<T>`
    - `add_assign`：执行加法赋值。
  - `impl Point<T>`
    - `sub`：执行减法。
  - `impl Point<T>`
    - `sub_assign`：执行减法赋值。
  - `impl Point<T>`
    - `to_f64`：转为 f64。
    - `to_i32`：转为 i32。
    - `to_i16`：转为 i16。
  - `impl Rotation`
    - `new`：创建实例。
  - `impl Point<f64>`
    - `rotate`：执行旋转。
    - `invert_rotation`：反转旋转方向。
  - `impl Line`
    - `from_points`：根据点集合构建。
    - `distance_from_point`：计算到点的距离。

### `policy.rs`
- 文件作用：负责策略相关逻辑。
- 结构体：
  - `PolicyTable`：策略表定义。
  - `PolicyInfo`：策略信息。
  - `PolicyGroupTable`：策略分组表定义。
  - `PolicyGroupInfo`：策略分组信息。
  - `PolicySetTable`：策略集表结构。
  - `PolicySetInfo`：策略集信息。
  - `GroupPolicyRelation`：分组策略关系定义。
  - `SetGroupRelation`：集分组关系定义。
  - `PolicySetEvaluator`：策略集Evaluator定义。
- `impl` 方法：
  - `impl a>`
    - `new`：创建实例。
    - `evaluate`：评估相关逻辑。

### `script_decision.rs`
- 文件作用：负责脚本decision相关逻辑。
- 结构体：
  - `ConditionLeaf`：ConditionLeaf定义。
  - `ConditionGroup`：条件分组存储结构。
  - `GuardDef`：GuardDef定义。
  - `PolicyDef`：策略Def定义。
  - `SubFlowDef`：Sub流程Def定义。
  - `Step`：步骤定义。
- 枚举：
  - `LogicOp`：Logic运算符枚举。
  - `ConditionNode`：条件节点定义。
  - `ActionRef`：动作Ref枚举。
  - `StepKind`：步骤类型。
- Trait：
  - `GuardRepository`：GuardRepository枚举。
  - `PolicyRepository`：策略Repository枚举。
  - `SubFlowRepository`：Sub流程Repository枚举。
- 顶层函数：
  - `load_common_guards`：加载commonguards。
  - `load_script_guards`：加载脚本guards。
  - `load_common_policies`：加载commonpolicies。
  - `load_script_policies`：加载脚本policies。
  - `load_common_subflows`：加载commonsubflows。
  - `load_script_subflows`：加载脚本subflows。

### `script_info.rs`
- 文件作用：负责脚本信息相关逻辑。
- 结构体：
  - `ScriptTable`：脚本表定义。
  - `ScriptInfo`：脚本信息。
  - `ScriptRuntimeSettings`：脚本运行时Settings定义。
- 枚举：
  - `ScriptType`：脚本Type类型。
  - `RuntimeType`：运行时Type类型。
  - `ScriptPlatform`：脚本平台枚举。
- `impl` 方法：
  - `impl ScriptTable`
    - `default`：提供默认值。
  - `impl ScriptInfo`
    - `default`：提供默认值。
  - `impl ScriptRuntimeSettings`
    - `default`：提供默认值。
  - `impl ScriptPlatform`
    - `default`：提供默认值。

### `script_task.rs`
- 文件作用：负责脚本任务相关逻辑。
- 结构体：
  - `ScriptTask`：脚本任务定义。
  - `ScriptTaskTable`：脚本任务表定义。
- 枚举：
  - `TaskRowType`：任务RowType类型。
  - `TaskTriggerMode`：任务Trigger模式。
  - `TaskTone`：任务Tone枚举。

### `script_variable.rs`
- 文件作用：负责脚本变量相关逻辑。
- 结构体：
  - `ScriptVariableCatalog`：脚本变量目录定义。
  - `ScriptVariableDef`：脚本变量Def定义。
- 枚举：
  - `ScriptVariableNamespace`：脚本变量Namespace枚举。
  - `ScriptVariableValueType`：脚本变量值Type类型。
  - `ScriptVariableSourceType`：脚本变量来源Type类型。
- `impl` 方法：
  - `impl ScriptVariableCatalog`
    - `default`：提供默认值。

## `src-tauri/crates/runtime_engine/src/domain`
### `vision.rs`
- 文件作用：负责视觉相关逻辑。

## `src-tauri/crates/runtime_engine/src`
### `infrastructure.rs`
- 文件作用：汇总并导出当前目录下的基础设施模块。

## `src-tauri/crates/runtime_engine/src/infrastructure`
### `adb_cli_local.rs`
- 文件作用：负责本地 ADB 命令相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure/adb_cli_local`
### `adb_command.rs`
- 文件作用：负责 ADB 命令构造相关逻辑。
- 枚举：
  - `ADBCommand`：ADBCommand枚举。
  - `ADBCmdConv`：ADBCmdConv枚举。
  - `ADBCommandResult`：ADBCommand结果。
- 顶层函数：
  - `sleep_cmd`：等待cmd。
  - `click_cmd`：处理点击cmd。
  - `long_click_cmd`：生成长按命令。
  - `long_click_and_swipe`：执行长按并滑动。
  - `swipe_cmd`：处理滑动cmd。
  - `swipe_duration_cmd`：处理滑动durationcmd。
  - `press_cmd`：生成按键命令。
  - `input_text_cmd`：处理输入textcmd。
  - `stop_app_cmd`：生成停止应用命令。
  - `adb_cmd_vec_to_string`：将 ADB 命令数组转为字符串。
- `impl` 方法：
  - `impl ADBCommand`
    - `fmt`：格式化输出。
  - `impl ADBCmdConv`
    - `fmt`：格式化输出。

### `adb_config.rs`
- 文件作用：负责ADB配置相关逻辑。
- 结构体：
  - `AdbServerConfig`：ADB 服务配置。
  - `AdbServerConnectName`：ADB 服务连接名称配置。
  - `AdbServerConnectIp`：ADB 服务连接 IP 配置。
  - `DirectUsbConnect`：USB 直连配置。
- 枚举：
  - `ADBConnectConfig`：ADB 连接配置。
- `impl` 方法：
  - `impl AdbServerConfig`
    - `valid`：校验有效性。
  - `impl AdbServerConnectName`
    - `valid`：校验有效性。
  - `impl AdbServerConnectIp`
    - `valid`：校验有效性。
  - `impl ADBConnectConfig`
    - `fmt`：格式化输出。
  - `impl ADBConnectConfig`
    - `valid`：校验有效性。
    - `update_adb_path`：处理更新ADB路径。
    - `update_server_addr`：处理更新服务addr。

### `adb_context.rs`
- 文件作用：负责ADB上下文相关逻辑。
- 结构体：
  - `ADBCtx`：ADBCtx定义。
- 顶层函数：
  - `get_adb_ctx`：读取 ADB 上下文。
- `impl` 方法：
  - `impl ADBCtx`
    - `fmt`：格式化输出。
  - `impl ADBCtx`
    - `new`：创建实例。
    - `send_adb_cmd`：发送ADBcmd。
    - `send_adb_loop_cmd`：发送ADBloopcmd。

### `adb_error.rs`
- 文件作用：负责ADB错误相关逻辑。
- 枚举：
  - `AdbError`：ADB错误枚举。

### `adb_executor.rs`
- 文件作用：负责 ADB 执行相关逻辑。
- 结构体：
  - `ADBExecutor`：ADBExecutor定义。
- `impl` 方法：
  - `impl ADBExecutor`
    - `fmt`：格式化输出。
  - `impl ADBExecutor`
    - `new`：创建实例。
    - `clone_for_child`：为子进程复制上下文。
    - `validate_config`：校验配置。
    - `run`：处理运行。
    - `high_speed_cmd`：生成高速截图命令。
    - `low_speed_cmd`：生成低速截图命令。
    - `translate_cmd`：转换单条命令。
    - `translate_sequence_cmd`：转换命令序列。
    - `execute_single`：执行单条命令。
    - `execute_adb_command`：执行 ADB 命令。
    - `execute_shell`：执行 Shell 命令。
    - `reconnect`：执行重连。
    - `try_to_connect`：尝试to连接。

## `src-tauri/crates/runtime_engine/src/infrastructure`
### `app_handle.rs`
- 文件作用：负责应用句柄相关逻辑。
- 顶层函数：
  - `get_app_handle`：读取应用句柄。
  - `init_app_handle`：初始化应用句柄。

### `capture.rs`
- 文件作用：负责截图相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure/capture`
### `capture_method.rs`
- 文件作用：负责截图方式相关逻辑。
- 枚举：
  - `CaptureMethod`：截图方式枚举。
- `impl` 方法：
  - `impl CaptureMethod`
    - `from`：从其他类型转换。
  - `impl CaptureMethod`
    - `fmt`：格式化输出。

### `window_cap.rs`
- 文件作用：负责窗口截图相关逻辑。
- 结构体：
  - `WindowInfo`：窗口信息。
- `impl` 方法：
  - `impl WindowInfo`
    - `init`：初始化相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure`
### `context.rs`
- 文件作用：负责上下文相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure/context`
### `child_process.rs`
- 文件作用：负责子进程运行环境初始化。
- 结构体：
  - `ChildProcessInitData`：子进程进程初始化数据定义。
- `impl` 方法：
  - `impl ChildProcessInitData`
    - `init_ort_env`：初始化 ONNX Runtime 环境。

### `child_process_manager.rs`
- 文件作用：负责子进程进程manager相关逻辑。
- 结构体：
  - `ChildProcessHandle`：子进程进程Handle定义。
  - `ChildProcessManager`：子进程进程Manager定义。
- 顶层函数：
  - `init_process_manager`：初始化进程manager。
  - `get_process_manager`：读取进程manager。
- `impl` 方法：
  - `impl ChildProcessManager`
    - `spawn_child`：启动子进程。
    - `stop_child`：停止子进程。
    - `restart_child`：重启子进程。
    - `is_running`：判断是否正在运行。
    - `get_running_device_ids`：读取运行中的设备标识列表。
    - `stop_all`：停止全部子进程。

### `child_process_sec.rs`
- 文件作用：负责子进程状态、IPC 客户端与取消控制。
- 枚举：
  - `RunningStatus`：Running状态。
- `impl` 方法：
  - `impl RunningStatus`
    - `from`：从其他类型转换。

### `init_error.rs`
- 文件作用：负责初始化错误相关逻辑。
- 枚举：
  - `InitError`：初始化错误枚举。

### `main_process.rs`
- 文件作用：负责主流程进程相关逻辑。
- 结构体：
  - `MainProcessCtx`：主流程进程Ctx定义。
- `impl` 方法：
  - `impl MainProcessCtx`
    - `new`：创建实例。
    - `init_scripts_mgr`：初始化脚本管理器。
    - `init_`：初始化相关逻辑。
    - `add_device_config`：追加设备配置。
    - `get_device_config`：读取设备配置。
    - `run`：处理运行。

## `src-tauri/crates/runtime_engine/src/infrastructure`
### `core.rs`
- 文件作用：负责核心逻辑。

### `db.rs`
- 文件作用：负责数据库相关逻辑。
- 顶层函数：
  - `init_db_with_path`：按指定路径初始化数据库。
  - `init_db`：初始化数据库。
  - `init_tables`：初始化数据表。
  - `ensure_device_script_schedule_columns`：确保设备脚本调度columns。
  - `ensure_script_tasks_columns`：确保脚本taskscolumns。
  - `ensure_script_time_template_values_schema`：确保脚本time模板values结构。
  - `rebuild_script_tasks_table`：重建脚本任务表。
  - `get_pool`：读取数据库连接池。
- `impl` 方法：
  - `impl DbRepo`
    - `delete`：删除数据。

### `devices.rs`
- 文件作用：负责设备相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure/devices`
### `adb_info.rs`
- 文件作用：负责ADB信息相关逻辑。
- 结构体：
  - `AdbInfo`：ADB 设备信息。
- 枚举：
  - `AdbConnectStatus`：ADB 连接状态。

### `device_adapter.rs`
- 文件作用：负责设备adapter相关逻辑。
- 结构体：
  - `AndroidDeviceAdapter`：Android设备Adapter定义。
- Trait：
  - `DeviceAdapter`：设备Adapter定义。
- 顶层函数：
  - `platform`：处理平台。
  - `valid_capture`：校验截图能力。
  - `capture_screen`：处理截图screen。
  - `click`：处理点击。
  - `swipe`：处理滑动。
  - `reboot`：重启设备。
  - `launch_app`：启动应用。
  - `stop_app`：停止应用。
  - `change_cap_method`：切换截图方式。
- `impl` 方法：
  - `impl AndroidDeviceAdapter`
    - `new`：创建实例。
  - `impl AndroidDeviceAdapter`
    - `platform`：处理平台。
    - `valid_capture`：校验截图能力。
    - `capture_screen`：处理截图screen。
    - `click`：处理点击。
    - `swipe`：处理滑动。
    - `reboot`：重启设备。
    - `launch_app`：启动应用。
    - `stop_app`：停止应用。
    - `change_cap_method`：切换截图方式。
  - `impl DesktopDeviceAdapter`
    - `new`：创建实例。
    - `unsupported`：返回不支持结果。
  - `impl DesktopDeviceAdapter`
    - `platform`：处理平台。
    - `valid_capture`：校验截图能力。
    - `capture_screen`：处理截图screen。
    - `click`：处理点击。
    - `swipe`：处理滑动。
    - `reboot`：重启设备。
    - `launch_app`：启动应用。
    - `stop_app`：停止应用。
    - `change_cap_method`：切换截图方式。

### `device_ctx.rs`
- 文件作用：负责设备ctx相关逻辑。
- 结构体：
  - `DeviceCtx`：设备Ctx定义。
- 顶层函数：
  - `get_device_ctx`：读取设备ctx。
  - `init_device_ctx`：初始化设备ctx。
- `impl` 方法：
  - `impl DeviceCtx`
    - `new`：创建实例。
    - `valid_capture`：校验截图能力。
    - `get_screenshot`：读取截图。
    - `change_cap_method`：切换截图方式。
    - `click`：处理点击。
    - `swipe`：处理滑动。
    - `reboot`：重启设备。
    - `launch_app`：启动应用。
    - `stop_app`：停止应用。

### `device_launcher.rs`
- 文件作用：负责设备launcher相关逻辑。
- 顶层函数：
  - `launch_device`：启动设备。
  - `try_connect_with_retry`：尝试连接withretry。
  - `probe_connection`：处理探针connection。

## `src-tauri/crates/runtime_engine/src/infrastructure`
### `hash_calculated.rs`
- 文件作用：负责哈希计算相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure/hash_calculated`
### `hash_error.rs`
- 文件作用：负责hash错误相关逻辑。
- 枚举：
  - `HashError`：Hash错误枚举。

### `model_file.rs`
- 文件作用：负责模型file相关逻辑。
- 顶层函数：
  - `get_hasher`：读取哈希器。
- `impl` 方法：
  - `impl XxHash64WithSeed`
    - `new`：创建实例。
  - `impl XxHash64WithSeed`
    - `finish`：完成计算并返回结果。
    - `write`：写入相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure`
### `http_client.rs`
- 文件作用：负责http客户端相关逻辑。
- 结构体：
  - `HttpClient`：Http客户端定义。
- 顶层函数：
  - `get_machine_code`：读取机器码。
- `impl` 方法：
  - `impl HttpClient`
    - `new`：创建实例。
    - `get_auth_session`：读取认证会话。
    - `get_jwt_token`：读取 JWT 令牌。
    - `set_auth_session`：设置认证会话。
    - `clear_auth_session`：清空认证会话。
    - `apply_auth_headers`：应用authheaders。
    - `send_request`：发送request。
    - `refresh_auth_session`：刷新auth会话。
    - `send_with_retry`：发送withretry。
    - `download_file`：下载文件。

### `image.rs`
- 文件作用：负责图像相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure/image`
### `save_image.rs`
- 文件作用：负责save图像相关逻辑。
- 顶层函数：
  - `save_screenshot`：保存截图。
  - `save_base64_image`：保存 Base64 图像。

## `src-tauri/crates/runtime_engine/src/infrastructure`
### `ipc.rs`
- 文件作用：负责 IPC 相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure/ipc`
### `chanel_server.rs`
- 文件作用：负责chanel服务相关逻辑。
- 结构体：
  - `IpcClientState`：IPC客户端状态。
- `impl` 方法：
  - `impl IpcServer`
    - `start`：处理启动。
    - `send_to_client`：发送to客户端。
    - `success_to_ui`：转换为界面成功结果。
    - `error_to_ui`：输出错误日志to界面。
  - `impl IpcServer`
    - `handle_msg`：处理收到的消息。

### `chanel_trait.rs`
- 文件作用：负责通道 Trait 定义。
- Trait：
  - `ChannelTrait`：处理通道Trait。
- 顶层函数：
  - `handle_msg`：处理收到的消息。

### `channel_error.rs`
- 文件作用：负责通道错误相关逻辑。

### `message.rs`
- 文件作用：负责消息相关逻辑。

### `msg_handler_main.rs`
- 文件作用：负责msghandler主流程相关逻辑。
- 顶层函数：
  - `handle_child_message`：处理子进程消息。
  - `handle_runtime_event`：处理运行时event。

## `src-tauri/crates/runtime_engine/src/infrastructure`
### `logging.rs`
- 文件作用：负责日志相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure/logging`
### `config.rs`
- 文件作用：负责配置相关逻辑。
- 结构体：
  - `LogMain`：日志主流程定义。
- `impl` 方法：
  - `impl LogMain`
    - `default`：提供默认值。
  - `impl LogMain`
    - `fmt`：格式化输出。

### `handler.rs`
- 文件作用：负责处理器相关逻辑。

### `log_cleaner.rs`
- 文件作用：负责日志cleaner相关逻辑。
- `impl` 方法：
  - `impl LogCleaner`
    - `set_retention_days`：设置保留天数。
    - `get_retention_days`：读取保留天数。
    - `start`：处理启动。
    - `clean_once`：执行单次清理。
    - `clean_now`：立即清理日志。

### `log_error.rs`
- 文件作用：负责日志错误相关逻辑。
- 枚举：
  - `LogError`：日志错误枚举。

### `logger.rs`
- 文件作用：负责日志器相关逻辑。
- 顶层函数：
  - `parse_log_level`：解析日志level。
- `impl` 方法：
  - `impl LogMain`
    - `update_level`：处理更新level。
    - `get_log_dir`：读取日志dir。
    - `update_log_dir`：处理更新日志dir。
  - `impl LogMain`
    - `init`：初始化相关逻辑。
    - `info_with_fields`：输出信息日志withfields。
    - `fn_begin`：记录函数开始。
    - `fn_end`：记录函数结束。
    - `info_with_tag`：输出信息日志with标签。
  - `impl LogMain`
    - `debug`：输出调试日志相关逻辑。
    - `info`：输出信息日志相关逻辑。
    - `warn`：输出警告日志相关逻辑。
    - `error`：输出错误日志相关逻辑。

### `main_process_log_handler.rs`
- 文件作用：负责主流程进程日志handler相关逻辑。
- 结构体：
  - `ChildLogReceiver`：子进程日志Receiver定义。
  - `DeviceLogWriter`：设备日志Writer定义。
- 顶层函数：
  - `init_child_log_receiver`：初始化子进程日志receiver。
  - `get_child_log_receiver`：读取子进程日志receiver。
- `impl` 方法：
  - `impl DeviceLogWriter`
    - `new`：创建实例。
    - `ensure_file`：确保file。
    - `write_log`：写入日志。
    - `set_log_to_file`：设置日志tofile。
  - `impl ChildLogReceiver`
    - `register_device`：注册设备。
    - `unregister_device`：注销设备。
    - `update_log_to_file`：处理更新日志tofile。
    - `handle_log`：处理日志。

## `src-tauri/crates/runtime_engine/src/infrastructure`
### `mail.rs`
- 文件作用：负责邮件发送相关逻辑。
- 结构体：
  - `EmailMessagePayload`：Email消息载荷。
- 顶层函数：
  - `load_email_config`：加载email配置。
  - `send_email`：发送email。
  - `send_stored_timeout_email`：发送stored超时email。
  - `build_transport`：构建传输器。
  - `validate_smtp_host_for_security`：校验smtphostforsecurity。
  - `build_mailbox`：构建邮箱。
  - `send_timeout_email_in_background`：发送超时emailinbackground。

### `ort.rs`
- 文件作用：负责ONNX Runtime相关逻辑。

### `path_resolve.rs`
- 文件作用：负责路径resolve相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure/path_resolve`
### `model_path.rs`
- 文件作用：负责模型路径相关逻辑。
- 顶层函数：
  - `resolve_model_path`：解析模型路径。
- `impl` 方法：
  - `impl PathUtil`
    - `resolve_path`：解析路径。

### `other_path.rs`
- 文件作用：负责other路径相关逻辑。
- `impl` 方法：
  - `impl PathUtil`
    - `sure_parent_exists`：确保父目录存在。
    - `get_absolute_path`：读取绝对路径。

### `path_error.rs`
- 文件作用：负责路径错误相关逻辑。
- 枚举：
  - `PathError`：路径错误枚举。

## `src-tauri/crates/runtime_engine/src/infrastructure`
### `scripts.rs`
- 文件作用：负责脚本相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure/scripts`
### `repository.rs`
- 文件作用：负责仓储访问相关逻辑。
- 结构体：
  - `ScriptMetaPage`：脚本Meta分页结果。
- Trait：
  - `ScriptRepository`：脚本Repository定义。
  - `CloudScriptRepository`：Cloud脚本Repository定义。
- 顶层函数：
  - `reload_index`：重载索引。
  - `page_metadata`：处理分页结果metadata。
  - `get_detail`：读取详情。
  - `save`：保存数据。
  - `delete`：删除数据。
  - `search`：执行搜索。
  - `download`：下载数据。

### `script_error.rs`
- 文件作用：负责脚本错误相关逻辑。
- 枚举：
  - `ScriptError`：脚本错误枚举。

### `script_info.rs`
- 文件作用：负责脚本信息相关逻辑。
- 结构体：
  - `ScriptInfo`：脚本信息。
  - `ScriptShow`：脚本Show定义。
- 枚举：
  - `RuntimeType`：运行时Type类型。
  - `ScriptType`：脚本Type类型。

### `script_info_model.rs`
- 文件作用：负责脚本信息模型相关逻辑。
- 结构体：
  - `ScriptPageReq`：脚本分页结果请求。
  - `ScriptFilter`：脚本过滤定义。
  - `ScriptPageResp`：脚本分页结果Resp定义。
  - `ScriptManager`：脚本Manager定义。
  - `ScriptMeta`：脚本Meta定义。
- 枚举：
  - `SortField`：Sort字段枚举。
  - `SortOrder`：SortOrder枚举。
- `impl` 方法：
  - `impl ScriptPageReq`
    - `default`：提供默认值。
  - `impl ScriptManager`
    - `empty`：返回空值。
  - `impl ScriptManager`
    - `new`：创建实例。
    - `get_scripts_page`：读取脚本分页结果。
    - `load_script_info`：加载脚本信息。
    - `load_from_file`：加载fromfile。
    - `matches_filter`：匹配过滤。
    - `sort_metadata`：整理元数据排序。
    - `get_ordering`：读取排序配置。
    - `load_from_directory`：加载fromdirectory。
    - `load_script_metadata_from_file`：加载脚本metadatafromfile。
    - `compute_last_modified`：计算最近修改时间。
    - `save_script`：保存脚本。
    - `delete_script`：删除脚本。
    - `get_scripts_with_complex_sort`：读取复杂排序后的脚本列表。

### `script_runtime.rs`
- 文件作用：负责脚本运行时相关逻辑。
- 结构体：
  - `ScriptRuntime`：脚本运行时定义。

## `src-tauri/crates/runtime_engine/src/infrastructure`
### `store_local.rs`
- 文件作用：负责本地存储相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure/store_local`
### `config_store.rs`
- 文件作用：负责配置store相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure`
### `vision.rs`
- 文件作用：负责视觉相关逻辑。

## `src-tauri/crates/runtime_engine/src/infrastructure/vision`
### `text_rec_cache.rs`
- 文件作用：负责text识别cache相关逻辑。
- 结构体：
  - `TextRecCacheEntry`：Text识别Cache条目。
  - `TextRecCacheDocument`：Text识别CacheDocument定义。
  - `TextRecCacheSessionStats`：Text识别Cache会话Stats定义。
  - `ScriptTextRecCacheRuntime`：脚本Text识别Cache运行时定义。
- 枚举：
  - `TextRecCacheError`：Text识别Cache错误枚举。
- 顶层函数：
  - `ensure_parent_dir`：确保parentdir。
  - `unix_timestamp_string`：生成 Unix 时间戳字符串。
  - `short_script_id`：处理短脚本id。
  - `sanitize_script_file_name`：清理脚本file名称。
  - `sanitize_empty_file_name`：清理emptyfile名称。
  - `sanitize_windows_reserved_name`：清理windowsreserved名称。
  - `sanitize_illegal_chars`：清理illegalchars。
- `impl` 方法：
  - `impl ScriptTextRecCacheRuntime`
    - `new`：创建实例。
    - `is_enabled`：判断是否启用。
    - `has_active_script`：判断当前脚本。
    - `apply_config`：应用配置。
    - `load_for_script`：加载for脚本。
    - `flush_current_script`：刷新当前脚本。
    - `find_entry`：查找条目。
    - `record_entry`：处理记录条目。
    - `current_document`：获取当前文档。
    - `resolve_cache_file_path`：解析缓存文件路径。
    - `read_document`：读取文档。
    - `reset_runtime_state`：重置运行时状态。

## `src-tauri/crates/runtime_engine/src`
### `lib.rs`
- 文件作用：负责库入口与公共导出。

## `src-tauri/crates/vision_core/src`
### `domain.rs`
- 文件作用：汇总并导出当前目录下的领域模块。

## `src-tauri/crates/vision_core/src/domain`
### `vision.rs`
- 文件作用：负责视觉相关逻辑。

## `src-tauri/crates/vision_core/src/domain/vision`
### `ocr_search.rs`
- 文件作用：负责 OCR 搜索相关逻辑。
- 结构体：
  - `VisionLayoutItem`：视觉布局项。
  - `VisionSnapshot`：视觉快照定义。
  - `SearchHit`：SearchHit定义。
  - `OcrSearcher`：OCR 搜索器。
- 枚举：
  - `VisionLayoutSource`：视觉布局来源枚举。
  - `RelativeAnchorType`：相对定位锚点类型。
  - `RelativeDirection`：相对定位方向。
  - `RelativeTargetKind`：相对定位目标类型。
  - `RelativeValueType`：相对定位取值类型。
  - `RelativeCompareOp`：相对定位比较运算符。
  - `LogicOp`：Logic运算符枚举。
  - `SearchScope`：Search作用域枚举。
  - `SearchRule`：搜索规则定义。
  - `PolicyConditionRule`：策略条件规则定义。
- 顶层函数：
  - `compare_ocr_items`：处理比较OCRitems。
  - `compare_det_items`：处理比较检测items。
  - `compare_layout`：处理比较布局。
  - `build_layout_items`：构建布局items。
  - `evaluate_relative_rule`：评估相对定位规则。
  - `match_anchor`：匹配锚点。
  - `matches_target_kind`：匹配目标类型。
  - `relative_score`：处理相对定位score。
  - `compare_relative_value`：处理比较相对定位值。
  - `compare_text`：处理比较text。
  - `compare_number`：处理比较number。
  - `extract_number`：提取数字。
  - `extract_fraction_number`：提取小数。
  - `test_text_only_search`：处理测试textonlysearch。
  - `test_yolo_only_rule_no_text_hit`：处理测试YOLOonly规则notexthit。
  - `test_not_yolo_rule`：处理测试notYOLO规则。
  - `test_deduplication`：处理测试deduplication。
  - `test_policy_condition_relative_rule_matches_number_on_right`：处理测试策略condition相对定位规则matchesnumberonright。
- `impl` 方法：
  - `impl VisionSnapshot`
    - `new`：创建实例。
    - `find_ocr_at`：查找指定位置的 OCR 结果。
  - `impl OcrSearcher`
    - `new`：创建实例。
    - `search`：执行搜索。
  - `impl SearchRule`
    - `evaluate`：评估相关逻辑。
    - `get_all_keywords`：读取全部keywords。
    - `collect_keywords`：收集keywords。
  - `impl PolicyConditionRule`
    - `evaluate`：评估相关逻辑。

### `result.rs`
- 文件作用：负责结果相关逻辑。
- 结构体：
  - `DetResult`：检测结果。
  - `OcrResult`：OCR 结果结构。
  - `BoundingBox`：边界框结构。
  - `StablePoint`：稳定点坐标结构。
  - `Rect`：矩形区域结构。
- 顶层函数：
  - `quantize_coord`：量化coord。
- `impl` 方法：
  - `impl DetResult`
    - `fmt`：格式化输出。
  - `impl DetResult`
    - `new`：创建实例。
  - `impl OcrResult`
    - `fmt`：格式化输出。
  - `impl OcrResult`
    - `new`：创建实例。
  - `impl BoundingBox`
    - `new`：创建实例。
    - `center`：计算中心点。
    - `to_stable_box`：转换为稳定边界框。
    - `to_stable_center`：转换为稳定中心点。
  - `impl StablePoint`
    - `to_stable`：转换为稳定坐标。

## `src-tauri/crates/vision_core/src`
### `infrastructure.rs`
- 文件作用：汇总并导出当前目录下的基础设施模块。

## `src-tauri/crates/vision_core/src/infrastructure`
### `core.rs`
- 文件作用：负责核心逻辑。

### `image.rs`
- 文件作用：负责图像相关逻辑。

## `src-tauri/crates/vision_core/src/infrastructure/image`
### `compression.rs`
- 文件作用：负责压缩处理相关逻辑。
- 枚举：
  - `ImageCompression`：图像Compression枚举。

### `crop_image.rs`
- 文件作用：负责裁剪图像相关逻辑。
- 顶层函数：
  - `get_crop_images`：读取裁剪images。
  - `get_crop_image`：读取裁剪图像。

### `img_error.rs`
- 文件作用：负责img错误相关逻辑。
- 枚举：
  - `ImageError`：图像错误枚举。

### `load_image.rs`
- 文件作用：负责load图像相关逻辑。
- 顶层函数：
  - `load_img_from_path`：加载imgfrom路径。
  - `dynamic_image_to_base64`：处理动态值图像to基础64。

## `src-tauri/crates/vision_core/src/infrastructure`
### `logging.rs`
- 文件作用：负责日志相关逻辑。

### `ort.rs`
- 文件作用：负责ONNX Runtime相关逻辑。

## `src-tauri/crates/vision_core/src/infrastructure/ort`
### `execution_provider_mgr.rs`
- 文件作用：负责执行提供方mgr相关逻辑。
- 结构体：
  - `ProviderConfigResult`：提供方配置结果。
- 枚举：
  - `InferenceBackend`：推理后端定义。
- 顶层函数：
  - `configure_or_switch_provider`：配置or切换提供方。
  - `try_enable_provider`：尝试启用提供方。
- `impl` 方法：
  - `impl InferenceBackend`
    - `fmt`：格式化输出。
  - `impl InferenceBackend`
    - `from_str`：从字符串解析。
    - `name`：返回名称相关逻辑。
    - `build_ep`：构建执行提供方。

### `ort_error.rs`
- 文件作用：负责ONNX Runtime错误相关逻辑。
- 枚举：
  - `OrtError`：ONNX Runtime 错误类型。

## `src-tauri/crates/vision_core/src/infrastructure`
### `vision.rs`
- 文件作用：负责视觉相关逻辑。

## `src-tauri/crates/vision_core/src/infrastructure/vision`
### `base_model.rs`
- 文件作用：负责基础模型相关逻辑。
- 结构体：
  - `BaseModel`：基础模型封装。
- 枚举：
  - `ModelSource`：模型来源枚举。
  - `ModelType`：模型Type类型。
- `impl` 方法：
  - `impl BaseModel`
    - `fmt`：格式化输出。
  - `impl BaseModel`
    - `resolve_builtin_model_path`：解析内置模型路径。
    - `new`：创建实例。
    - `inference_base`：处理推理基础。

### `base_traits.rs`
- 文件作用：负责基础traits相关逻辑。
- Trait：
  - `ModelHandler`：处理模型Handler。
  - `TextDetector`：文本检测器封装。
  - `TextRecognizer`：文本识别器封装。
- 顶层函数：
  - `load_model`：加载模型。
  - `get_input_size`：读取输入size。
  - `preprocess`：预处理相关逻辑。
  - `inference`：处理推理。
  - `get_input_node_name`：读取输入节点名称。
  - `get_output_node_name`：读取输出节点名称。
  - `get_target_width`：读取目标width。
  - `get_target_height`：读取目标height。
  - `detect`：执行检测相关逻辑。
  - `postprocess`：后处理相关逻辑。
  - `recognize_preprocessed_inputs`：执行识别preprocessedinputs。
  - `recognize`：执行识别相关逻辑。
  - `recognize_batch`：执行识别批量。
  - `preprocess_batch`：预处理批量。
  - `postprocess_batch`：后处理批量。

### `color_analyzer.rs`
- 文件作用：负责颜色analyzer相关逻辑。
- 枚举：
  - `ColorTag`：颜色标签枚举。
- `impl` 方法：
  - `impl ColorTag`
    - `as_str`：返回字符串表示。
  - `impl ColorAnalyzer`
    - `analyze_box`：处理分析框。
    - `rgb_to_tag`：将 RGB 转为颜色标签。
    - `most_frequent`：统计最高频项。

### `det.rs`
- 文件作用：负责检测相关逻辑。
- 枚举：
  - `DetectorType`：检测器Type类型。

## `src-tauri/crates/vision_core/src/infrastructure/vision/det`
### `paddle_dbnet.rs`
- 文件作用：负责 Paddle DBNet 检测相关逻辑。
- 结构体：
  - `PaddleDetDbNet`：Paddle 文本检测模型封装。
- 顶层函数：
  - `get_bounding_rect`：读取边界矩形区域。
  - `box_score_fast`：处理框score快速。
  - `point_in_polygon`：处理点in多边形。
  - `unclip_polygon`：扩展多边形。
  - `scale_polygon`：缩放多边形。
- `impl` 方法：
  - `impl PaddleDetDbNet`
    - `load_model`：加载模型。
    - `get_input_size`：读取输入size。
    - `preprocess`：预处理相关逻辑。
    - `inference`：处理推理。
    - `get_input_node_name`：读取输入节点名称。
    - `get_output_node_name`：读取输出节点名称。
    - `get_target_width`：读取目标width。
    - `get_target_height`：读取目标height。
  - `impl PaddleDetDbNet`
    - `postprocess`：后处理相关逻辑。

### `yolo.rs`
- 文件作用：负责 YOLO 检测相关逻辑。
- 结构体：
  - `YoloDet`：YOLO 检测模型封装。
  - `YoloCandidate`：YOLO 候选框结构。
- 枚举：
  - `YoloPostprocessKind`：YOLO 后处理类型。
  - `YoloOutputLayout`：YOLO 输出布局。
  - `YoloBoxFormat`：YOLO 边界框格式。
- 顶层函数：
  - `intersection`：计算交集。
  - `union`：计算并集。
  - `calculate_iou`：计算交并比。
  - `apply_nms`：应用非极大值抑制。
  - `build_detector`：构建检测器。
  - `parses_yolo11_raw_head_output`：解析YOLO11rawhead输出。
  - `parses_yolo26_end_to_end_output_without_nms`：解析YOLO26endtoend输出without非极大值抑制。
  - `filters_by_txt_idx_for_text_detection`：按文本索引过滤文本检测结果。
- `impl` 方法：
  - `impl YoloPostprocessKind`
    - `default`：提供默认值。
  - `impl YoloDet`
    - `default_preprocess_buffer`：创建默认预处理缓冲区。
    - `refresh_runtime_config`：刷新运行时配置。
    - `load_labels`：加载labels。
    - `model_file_stem`：处理模型filestem。
    - `detect_output_layout`：执行检测输出布局。
    - `resolve_output_layout`：解析输出布局。
    - `resolve_label`：解析标签。
    - `fill_chw_buffer`：填充chwbuffer。
    - `finalize_candidates`：整理candidates。
    - `candidate_xyxy`：处理候选框xyxy。
    - `candidate_to_bounding_box`：处理候选框to边界框。
    - `allow_class`：判断类别是否允许。
    - `clamp_x`：限制x。
    - `clamp_y`：限制y。
    - `build_xywh_box`：构建 XYWH 边界框。
    - `build_xyxy_box`：构建 XYXY 边界框。
    - `postprocess_legacy`：执行旧版后处理。
    - `postprocess_end_to_end`：执行端到端后处理。
  - `impl YoloDet`
    - `load_model`：加载模型。
    - `get_input_size`：读取输入size。
    - `preprocess`：预处理相关逻辑。
    - `inference`：处理推理。
    - `get_input_node_name`：读取输入节点名称。
    - `get_output_node_name`：读取输出节点名称。
    - `get_target_width`：读取目标width。
    - `get_target_height`：读取目标height。
  - `impl YoloDet`
    - `detect`：执行检测相关逻辑。
    - `postprocess`：后处理相关逻辑。

## `src-tauri/crates/vision_core/src/infrastructure/vision`
### `ocr_service.rs`
- 文件作用：负责 OCR 服务相关逻辑。
- 结构体：
  - `OcrService`：OCRService定义。
  - `RealOcrTestConfig`：OCR 测试配置。
- 顶层函数：
  - `required_env`：读取必需环境变量。
  - `real_ocr_test_config`：处理真实OCR测试配置。
  - `real_paths_detector_init_and_detect`：执行真实路径下的检测器初始化与检测。
  - `real_paths_ocr_init_and_recognize`：执行真实路径下的 OCR 初始化与识别。
- `impl` 方法：
  - `impl OcrService`
    - `new`：创建实例。
    - `init_detector`：初始化检测器。
    - `init_recognizer`：初始化识别器。
    - `set_detector_instance`：设置检测器instance。
    - `set_recognizer_instance`：设置识别器instance。
    - `detect`：执行检测相关逻辑。
    - `recognize`：执行识别相关逻辑。
    - `recognize_batch`：执行识别批量。
    - `ocr`：执行 OCR相关逻辑。
    - `ocr_batch`：执行 OCR批量。
    - `is_ready`：判断是否就绪。
    - `detector_info`：处理检测器信息。
    - `recognizer_info`：处理识别器信息。
  - `impl OcrService`
    - `default`：提供默认值。

### `rec.rs`
- 文件作用：负责识别相关逻辑。
- 枚举：
  - `RecognizerType`：识别器Type类型。

## `src-tauri/crates/vision_core/src/infrastructure/vision/rec`
### `paddle_crnn.rs`
- 文件作用：负责 Paddle CRNN 识别相关逻辑。
- 结构体：
  - `RecSample`：识别Sample定义。
  - `PaddleRecCrnn`：Paddle 文本识别模型封装。
- 枚举：
  - `RecResizeFilter`：识别Resize过滤枚举。
  - `RecProcessingMode`：识别Processing模式。
- `impl` 方法：
  - `impl RecResizeFilter`
    - `default`：提供默认值。
  - `impl RecProcessingMode`
    - `default`：提供默认值。
  - `impl PaddleRecCrnn`
    - `default_micro_batch_size`：返回默认微批量大小。
    - `default_width_bucket_step`：返回默认宽度分桶步长。
    - `active_filter`：处理当前过滤。
    - `align_width`：对齐宽度。
    - `calc_target_width`：计算目标宽度。
    - `bucket_width`：计算宽度分桶。
    - `resolve_dict_path`：解析字典路径。
    - `fill_image_tensor`：填充图像tensor。
    - `collect_samples`：收集samples。
    - `recognize_single_samples`：执行识别singlesamples。
    - `preprocess_sample_batch`：预处理样本批量数据。
    - `recognize_micro_batches`：执行识别microbatches。
    - `load_dict`：加载dict。
  - `impl PaddleRecCrnn`
    - `load_model`：加载模型。
    - `get_input_size`：读取输入size。
    - `preprocess`：预处理相关逻辑。
    - `inference`：处理推理。
    - `get_input_node_name`：读取输入节点名称。
    - `get_output_node_name`：读取输出节点名称。
    - `get_target_width`：读取目标width。
    - `get_target_height`：读取目标height。
  - `impl PaddleRecCrnn`
    - `recognize`：执行识别相关逻辑。
    - `postprocess`：后处理相关逻辑。
    - `preprocess_batch`：预处理批量。
    - `postprocess_batch`：后处理批量。
    - `recognize_batch`：执行识别批量。

## `src-tauri/crates/vision_core/src/infrastructure/vision`
### `tensor_view.rs`
- 文件作用：负责张量视图相关逻辑。

### `vision_error.rs`
- 文件作用：负责视觉错误相关逻辑。
- 枚举：
  - `VisionError`：视觉错误枚举。

## `src-tauri/crates/vision_core/src`
### `lib.rs`
- 文件作用：负责库入口与公共导出。

## `src-tauri/src`
### `api.rs`
- 文件作用：负责接口相关逻辑。

## `src-tauri/src/api`
### `api_response.rs`
- 文件作用：负责接口响应相关逻辑。

### `backend_cmd.rs`
- 文件作用：负责后端cmd相关逻辑。
- 结构体：
  - `UploadAuthor`：UploadAuthor定义。
- 顶层函数：
  - `backend_send_verification_code`（Tauri 命令）：发送验证码。
  - `backend_register`（Tauri 命令）：注册账号。
  - `backend_reset_password`（Tauri 命令）：重置密码。
  - `backend_login`（Tauri 命令）：执行登录。
  - `backend_get_auth_session`（Tauri 命令）：读取认证会话。
  - `backend_logout`（Tauri 命令）：退出登录。
  - `backend_get_profile`（Tauri 命令）：读取用户资料。
  - `backend_search_scripts`（Tauri 命令）：搜索脚本。
  - `backend_redeem_sponsor_code`（Tauri 命令）：兑换赞助码。
  - `backend_update_username`（Tauri 命令）：更新用户名。
  - `backend_check_update`（Tauri 命令）：检查更新。
  - `backend_download_script`（Tauri 命令）：下载脚本。
  - `backend_upload_script`（Tauri 命令）：上传脚本。
  - `backend_upload_model`（Tauri 命令）：上传模型。
  - `backend_download_model`（Tauri 命令）：下载模型。
  - `trans_api_res_token`：转换接口响应令牌。
  - `fetch_upload_author`：读取上传作者信息。

### `backend_dto.rs`
- 文件作用：负责后端dto相关逻辑。
- 结构体：
  - `LoginReq`：登录请求。
  - `RegisterReq`：Register请求。
  - `ResetPasswordReq`：重置密码请求。
  - `ScriptSearchReq`：脚本Search请求。
  - `SponsorRedeemReq`：赞助码兑换请求。
  - `TauriUpdateRes`：更新结果响应。
  - `PlatformUpdateInfo`：平台更新信息。
  - `PageRes`：分页结果响应。
  - `ScriptUploadRequest`：脚本UploadRequest请求。
  - `UpdateUsernameReq`：更新Username请求。

### `dev_test.rs`
- 文件作用：负责dev测试相关逻辑。
- 顶层函数：
  - `describe_adb_config_issue`：生成说明ADB配置issue。
  - `validate_capture_request`：校验截图request。
  - `resolve_scripts_dir`：解析脚本目录。
  - `resolve_custom_model_path`：解析自定义模型路径。
  - `resolve_optional_relative_path`：解析可选相对路径。
  - `normalize_detector_conf`：规范化检测器配置。
  - `normalize_recognizer_conf`：规范化识别器配置。
  - `dev_capture_test`（Tauri 命令）：执行开发环境截图测试。
  - `save_captured_image`（Tauri 命令）：保存capturedimage。
  - `yolo_inference_test`（Tauri 命令）：执行 YOLO 推理测试。
  - `yolo_inference_image_data_test`（Tauri 命令）：执行图像数据 YOLO 推理测试。
  - `paddle_ocr_inference_test`（Tauri 命令）：执行 Paddle OCR 推理测试。
  - `paddle_ocr_inference_image_data_test`（Tauri 命令）：执行图像数据 Paddle OCR 推理测试。

### `domain.rs`
- 文件作用：汇总并导出当前目录下的领域模块。

## `src-tauri/src/api/domain`
### `devices.rs`
- 文件作用：负责设备相关逻辑。
- 顶层函数：
  - `reconcile_runtime_after_device_save`：同步运行时after设备save。
  - `get_all_devices_cmd`（Tauri 命令）：读取全部设备。
  - `get_device_by_id_cmd`（Tauri 命令）：读取设备id。
  - `save_device_cmd`（Tauri 命令）：保存devicecmd。
  - `delete_device_cmd`（Tauri 命令）：删除devicecmd。
  - `get_cpu_count_cmd`（Tauri 命令）：读取 CPU 数量。

### `policy.rs`
- 文件作用：负责策略相关逻辑。
- 顶层函数：
  - `get_all_policies_cmd`（Tauri 命令）：读取全部策略。
  - `save_policy_cmd`（Tauri 命令）：保存policycmd。
  - `delete_policy_cmd`（Tauri 命令）：删除policycmd。
  - `get_all_policy_groups_cmd`（Tauri 命令）：读取全部策略分组。
  - `save_policy_group_cmd`（Tauri 命令）：保存policygroup。
  - `delete_policy_group_cmd`（Tauri 命令）：删除policygroup。
  - `get_group_policies_cmd`（Tauri 命令）：读取分组policies。
  - `update_group_policies_cmd`（Tauri 命令）：更新分组policies。
  - `get_all_policy_sets_cmd`（Tauri 命令）：读取全部策略集。
  - `save_policy_set_cmd`（Tauri 命令）：保存policyset。
  - `delete_policy_set_cmd`（Tauri 命令）：删除policyset。
  - `get_set_groups_cmd`（Tauri 命令）：读取策略集分组。
  - `update_set_groups_cmd`（Tauri 命令）：更新策略集分组。

### `schedule.rs`
- 文件作用：负责调度相关逻辑。
- 顶层函数：
  - `device_platform_label`：处理设备平台标签。
  - `script_platform_label`：处理脚本平台标签。
  - `platform_matches`：处理平台matches。
  - `validate_assignment_platform`：校验分配平台。
  - `get_assignments_by_device_cmd`（Tauri 命令）：读取设备分配记录。
  - `save_assignment_cmd`（Tauri 命令）：保存assignmentcmd。
  - `delete_assignment_cmd`（Tauri 命令）：删除assignmentcmd。
  - `reorder_assignments_cmd`（Tauri 命令）：重排设备分配顺序。
  - `get_schedules_by_device_cmd`（Tauri 命令）：读取设备调度配置。
  - `clear_schedules_cmd`（Tauri 命令）：清空schedulescmd。
  - `clear_schedules_by_script_cmd`（Tauri 命令）：清空schedulesby。
  - `get_all_time_templates_cmd`（Tauri 命令）：读取全部时间模板。
  - `save_time_template_cmd`（Tauri 命令）：保存timetemplate。
  - `delete_time_template_cmd`（Tauri 命令）：删除timetemplate。
  - `normalize_account_id`：规范化账号标识。
  - `find_script_time_template_values_exact`：精确查找脚本时间模板值。
  - `get_script_time_template_values_cmd`（Tauri 命令）：读取脚本时间模板values。
  - `save_script_time_template_values_cmd`（Tauri 命令）：保存scripttime。
  - `delete_script_time_template_values_cmd`（Tauri 命令）：删除scripttime。

### `script_batch_insert.rs`
- 文件作用：负责脚本批量insert相关逻辑。
- 顶层函数：
  - `batch_insert_script_related`：批量插入脚本关联数据。

### `scripts.rs`
- 文件作用：负责脚本相关逻辑。
- 顶层函数：
  - `get_all_scripts_cmd`（Tauri 命令）：读取全部脚本。
  - `get_script_by_id_cmd`（Tauri 命令）：读取脚本id。
  - `save_script_cmd`（Tauri 命令）：保存scriptcmd。
  - `delete_script_cmd`（Tauri 命令）：删除scriptcmd。
  - `get_script_tasks_cmd`（Tauri 命令）：读取脚本tasks。
  - `save_script_tasks_cmd`（Tauri 命令）：保存scripttasks。
  - `get_yolo_labels_cmd`（Tauri 命令）：读取 YOLO 标签。
  - `clone_local_script_cmd`（Tauri 命令）：克隆localscript。

## `src-tauri/src/api`
### `dto.rs`
- 文件作用：负责数据传输对象定义。

### `infrastructure.rs`
- 文件作用：汇总并导出当前目录下的基础设施模块。
- 顶层函数：
  - `get_uuid_v7`（Tauri 命令）：读取 UUID。

## `src-tauri/src/api/infrastructure`
### `config.rs`
- 文件作用：负责配置相关逻辑。

## `src-tauri/src/api/infrastructure/config`
### `email.rs`
- 文件作用：负责邮件配置相关逻辑。
- 顶层函数：
  - `get_email_config_cmd`（Tauri 命令）：读取邮件配置。
  - `set_email_config_cmd`（Tauri 命令）：设置邮件配置。
  - `send_test_email_cmd`（Tauri 命令）：发送testemail。

### `log_api.rs`
- 文件作用：负责日志api相关逻辑。
- 顶层函数：
  - `update_log_level_cmd`（Tauri 命令）：更新日志level。
  - `update_log_dir_cmd`（Tauri 命令）：更新日志目录。
  - `update_retention_days_cmd`（Tauri 命令）：更新保留期days。
  - `get_log_config_cmd`（Tauri 命令）：读取日志配置。
  - `clean_logs_now_cmd`（Tauri 命令）：处理清理logsnow。
  - `update_child_log_level_cmd`（Tauri 命令）：更新child日志level。

### `short_cut.rs`
- 文件作用：负责快捷键相关逻辑。

### `sys_conf.rs`
- 文件作用：负责sys配置相关逻辑。
- 顶层函数：
  - `set_system_settings_cmd`（Tauri 命令）：设置系统设置。

### `vision_cache.rs`
- 文件作用：负责视觉cache相关逻辑。
- 顶层函数：
  - `get_vision_text_cache_config_cmd`（Tauri 命令）：读取视觉文本缓存配置。
  - `set_vision_text_cache_config_cmd`（Tauri 命令）：设置视觉文本缓存配置。

## `src-tauri/src/api/infrastructure`
### `frontend_debug.rs`
- 文件作用：负责前端调试相关逻辑。
- 顶层函数：
  - `frontend_debug_log_cmd`（Tauri 命令）：上报前端调试日志。

### `img.rs`
- 文件作用：负责图像处理相关逻辑。
- 顶层函数：
  - `convert_img_to_base64_cmd`（Tauri 命令）：转换imgto。

### `process_api.rs`
- 文件作用：负责进程api相关逻辑。
- 结构体：
  - `LoadedScriptBundle`：已加载脚本包。
- 顶层函数：
  - `load_device_table`：加载设备表。
  - `validate_runtime_platform_supported`：校验运行时平台supported。
  - `load_vision_text_cache_runtime_config`：加载视觉textcache运行时配置。
  - `to_runtime_policy`：转换为运行时策略。
  - `map_timeout_action`：映射超时动作。
  - `map_timeout_notify_channel`：映射超时notify通道。
  - `normalize_account_id`：规范化账号标识。
  - `find_template_values_with_fallback`：查找带兜底逻辑的模板值。
  - `load_script_bundle`：加载脚本包。
  - `load_runtime_queue`：加载运行时队列。
  - `build_runtime_queue_item`：构建运行时队列项。
  - `load_debug_scope_queue_item`：加载调试作用域队列项。
  - `load_script_bundles`：加载脚本bundles。
  - `build_debug_template_values_json`：构建调试模板valuesJSON。
  - `validate_recovery_task_config`：校验恢复任务配置。
  - `validate_run_target_support`：校验运行目标支持。
  - `load_runtime_session_for_target`：加载运行时会话for目标。
  - `restart_device_runtime_internal`：重启设备运行时内部流程。
  - `build_runtime_session_snapshot`：构建运行时会话快照。
  - `send_session_control`：发送会话控制。
  - `send_process_control`：发送进程控制。
  - `wait_for_ipc_client`：处理等待forIPC客户端。
  - `prepare_device_launch`：准备设备启动参数。
  - `build_child_init_data`：构建子进程初始化数据。
  - `ensure_device_online`：确保设备online。
  - `cmd_device_start`（Tauri 命令）：处理设备启动。
  - `cmd_device_stop`（Tauri 命令）：处理设备停止。
  - `cmd_device_pause`（Tauri 命令）：处理设备暂停。
  - `cmd_sync_device_runtime_session`（Tauri 命令）：处理同步设备运行时会话。
  - `cmd_run_script_target`（Tauri 命令）：运行指定脚本目标。
  - `cmd_restart_device_runtime`（Tauri 命令）：处理重启设备运行时。
  - `cmd_device_shutdown`（Tauri 命令）：处理设备关闭。
  - `cmd_get_running_devices`（Tauri 命令）：处理读取运行状态devices。
  - `cmd_spawn_device`（Tauri 命令）：处理派生设备。
  - `cmd_prepare_device_capture`（Tauri 命令）：处理准备设备截图。
  - `cmd_is_device_running`（Tauri 命令）：判断设备是否正在运行。

### `runtime_sync.rs`
- 文件作用：负责运行时sync相关逻辑。
- 顶层函数：
  - `parse_device_id`：解析设备id。
  - `load_assigned_device_ids_by_script`：加载assigned设备idsby脚本。
  - `load_assigned_device_ids_by_time_template`：加载assigned设备idsbytime模板。
  - `sync_device_session_if_online`：在线时同步设备会话。
  - `sync_device_sessions_if_online`：在线时同步全部设备会话。

### `vision_lab.rs`
- 文件作用：负责视觉lab相关逻辑。
- 结构体：
  - `VisionLabModelConfig`：视觉Lab模型配置。
- 顶层函数：
  - `is_supported_image`：判断是否为支持的图像格式。
  - `sanitize_file_name`：清理file名称。
  - `ensure_unique_path`：确保unique路径。
  - `build_output_name`：构建输出名称。
  - `vision_lab_model_config_path`：处理视觉lab模型配置路径。
  - `get_vision_lab_model_config_cmd`（Tauri 命令）：读取视觉lab模型配置。
  - `set_vision_lab_model_config_cmd`（Tauri 命令）：设置视觉lab模型配置。
  - `vision_list_image_files_cmd`（Tauri 命令）：处理视觉list图片文件。
  - `vision_save_capture_image_cmd`（Tauri 命令）：处理视觉保存截图图片。

## `src-tauri/src`
### `app.rs`
- 文件作用：负责应用相关逻辑。

## `src-tauri/src/app`
### `app_error.rs`
- 文件作用：负责app错误相关逻辑。

### `before_exit.rs`
- 文件作用：负责退出前处理相关逻辑。
- 顶层函数：
  - `before_exit`：执行退出前处理。

### `config.rs`
- 文件作用：负责配置相关逻辑。

## `src-tauri/src/app/config`
### `email_conf.rs`
- 文件作用：负责email配置相关逻辑。
- 顶层函数：
  - `get_email_config_app`：读取邮件配置。
  - `set_email_config_app`：设置邮件配置。
  - `send_test_email_app`：发送测试emailapp。
  - `normalize_email_config`：规范化邮件配置。

### `log_conf.rs`
- 文件作用：负责日志配置相关逻辑。
- 顶层函数：
  - `persist_log_config_to_store`：持久化日志配置tostore。
  - `update_log_level_app`：处理更新日志levelapp。
  - `update_log_dir_app`：处理更新日志dirapp。
  - `update_retention_days_app`：处理更新retentiondaysapp。
  - `get_log_config_app`：读取日志配置app。
  - `clean_logs_now_app`：立即清理日志。
  - `update_child_log_level_app`：处理更新子进程日志levelapp。

### `short_cut.rs`
- 文件作用：负责快捷键相关逻辑。
- 顶层函数：
  - `register_short_cut_by_config`：按配置注册快捷键。
  - `unregister_all`：注销全部快捷键。
  - `toggle_window_handler`：切换窗口handler。
  - `toggle_all_scripts_handler`：切换全部scriptshandler。
  - `capture_shortcut_handler`：处理截图快捷键handler。

### `sys_conf.rs`
- 文件作用：负责sys配置相关逻辑。
- 顶层函数：
  - `set_system_settings_app`：设置系统settingsapp。
  - `handle_always_on_top_setting`：处理始终on置顶setting。
  - `handle_auto_start_setting`：处理自动启动setting。

### `vision_cache_conf.rs`
- 文件作用：负责视觉cache配置相关逻辑。
- 顶层函数：
  - `get_vision_text_cache_config_app`：读取视觉textcache配置app。
  - `set_vision_text_cache_config_app`：设置视觉textcache配置app。

## `src-tauri/src/app`
### `dev_test.rs`
- 文件作用：负责dev测试相关逻辑。
- 顶层函数：
  - `load_img_from_base64`：加载imgfrom基础64。
  - `yolo_infer_test`：执行 YOLO 推理测试。
  - `paddle_ocr_infer`：执行 Paddle OCR 推理。
  - `yolo_infer_base64_test`：执行 Base64 YOLO 推理测试。
  - `paddle_ocr_base64_infer`：执行 Base64 Paddle OCR 推理。

### `init_start.rs`
- 文件作用：负责初始化启动相关逻辑。
- 顶层函数：
  - `init_at_start`：初始化启动时启动。
  - `init_autostart`：初始化开机自启。
  - `init_short_cut_by_config`：初始化快捷键by配置。
  - `init_resources_path`：初始化资源路径。
  - `init_start_model`：初始化启动模型。
  - `init_conf_async`：初始化配置异步。

## `src-tauri/src`
### `common.rs`
- 文件作用：负责公共逻辑。

### `constant.rs`
- 文件作用：负责常量定义。

### `domain.rs`
- 文件作用：汇总并导出当前目录下的领域模块。

## `src-tauri/src/domain`
### `config.rs`
- 文件作用：负责配置相关逻辑。

### `devices.rs`
- 文件作用：负责设备相关逻辑。

### `schedule.rs`
- 文件作用：负责调度相关逻辑。

### `scripts.rs`
- 文件作用：负责脚本相关逻辑。

### `vision.rs`
- 文件作用：负责视觉相关逻辑。

## `src-tauri/src`
### `infrastructure.rs`
- 文件作用：汇总并导出当前目录下的基础设施模块。

## `src-tauri/src/infrastructure`
### `adb_cli_local.rs`
- 文件作用：负责本地 ADB 命令相关逻辑。

### `app_handle.rs`
- 文件作用：负责应用句柄相关逻辑。

### `capture.rs`
- 文件作用：负责截图相关逻辑。

### `context.rs`
- 文件作用：负责上下文相关逻辑。

### `core.rs`
- 文件作用：负责核心逻辑。

### `db.rs`
- 文件作用：负责数据库相关逻辑。

### `devices.rs`
- 文件作用：负责设备相关逻辑。

### `http_client.rs`
- 文件作用：负责http客户端相关逻辑。

### `image.rs`
- 文件作用：负责图像相关逻辑。

### `ipc.rs`
- 文件作用：负责 IPC 相关逻辑。

### `logging.rs`
- 文件作用：负责日志相关逻辑。

### `ort.rs`
- 文件作用：负责ONNX Runtime相关逻辑。

### `path_resolve.rs`
- 文件作用：负责路径resolve相关逻辑。

### `scripts.rs`
- 文件作用：负责脚本相关逻辑。

### `store_local.rs`
- 文件作用：负责本地存储相关逻辑。

### `vision.rs`
- 文件作用：负责视觉相关逻辑。

## `src-tauri/src`
### `lib.rs`
- 文件作用：负责库入口与公共导出。
- 顶层函数：
  - `run`：处理运行。

### `main.rs`
- 文件作用：负责主流程相关逻辑。
- 顶层函数：
  - `main`：应用入口。

### `main_child.rs`
- 文件作用：负责主流程子进程相关逻辑。
- 枚举：
  - `ChildProcessError`：子进程错误类型。
- 顶层函数：
  - `main`：应用入口。
  - `run_child_process`：处理运行子进程进程。
  - `run_main_loop`：处理运行主流程loop。
